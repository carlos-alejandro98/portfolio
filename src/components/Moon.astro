<div class="river flex max-[768px]:hidden">
    <div class="moon"></div>
</div>

<style>
    .river {
        width: 80px;
        height: 80px;
        align-items: center;
        justify-content: center;
        background: radial-gradient(circle, #6276c9, #343064);
        position: fixed;
        top: 1rem;
        left: 1rem;
        overflow: hidden;
        border-radius: 100%;
        z-index: 2;
    }
    .river::before,
    .river::after {
        content: "";
        position: absolute;
        display: block;
        background-color: rgba(172, 193, 236, 0.7);
        border-radius: 40%;
        -webkit-animation: wave 10s linear infinite;
        animation: wave 10s linear infinite;
    }
    .river::before {
        width: 180%;
        height: 180%;
        top: 70%;
        left: -40%;
    }
    .river::after {
        width: 200%;
        height: 200%;
        top: 75%;
        left: -50%;
        -webkit-animation-delay: 0.8s;
        animation-delay: 0.8s;
    }

    .moon {
        width: 60px;
        height: 60px;
        border-radius: 50%;
        z-index: -1;
        box-shadow:
            inset -30px -20px 10px -15px white,
            inset -30px -20px 0 -1px #181817;
        position: relative;
    }
    .moon::after {
        content: "";
        position: absolute;
        border-radius: 50%;
        width: 1px;
        height: 1px;
        box-shadow:
            10px 20px 0 1px rgba(255, 255, 255, 0.5),
            50px -10px 0 1px rgba(255, 255, 255, 0.3),
            120px 50px 0 1px rgba(255, 255, 255, 0.3),
            0px 60px 0 1px rgba(255, 255, 255, 0.2),
            -10px -20px 0 1px rgba(255, 255, 255, 0.6),
            30px 40px 0 1px rgba(255, 255, 255, 0.1),
            130px 30px 0 1px rgba(255, 255, 255, 0.1);
    }

    @-webkit-keyframes wave {
        to {
            transform: rotate(1turn);
        }
    }

    @keyframes wave {
        to {
            transform: rotate(1turn);
        }
    }
</style>
